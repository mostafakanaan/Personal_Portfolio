# Environment Variables Example
# Copy this file to .env.local for local development

# LLM API Key (Optional - only needed if using external LLM service)
# WARNING: Never commit .env.local or expose API keys in client-side code
# VITE_LLM_KEY=your_llm_api_key_here

# Note: VITE_ prefix exposes variables to the client bundle.
# Only use VITE_ prefix for non-sensitive configuration.
# Sensitive keys should be handled server-side only.
